export type Alignment = {
  camber: string,
  para: string,
  chasse: string
}

export type Springs = {
  springs: string,
  gas: string
}

export type ShockAbsorber = {
  det: string,
  amort: string
}

export type Differential = {
  front: string | null,
  rear: string,
  center: string | null
}
